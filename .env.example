# Copy to .env.local and fill in. NEVER commit .env.local.
ANTHROPIC_API_KEY=      # from console.anthropic.com
GOOGLE_CLIENT_ID=       # Google Cloud Console -> Credentials -> OAuth 2.0 Client ID (type: Chrome Extension)
