{
  "name": "catchup-proxy",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "secret:anthropic": "wrangler secret put ANTHROPIC_API_KEY",
    "secret:proxy": "wrangler secret put PROXY_SHARED_SECRET"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
